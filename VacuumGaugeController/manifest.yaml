# ARES Device Driver Manifest
# Target: Vacuum Gauge Controller Remastered

# Basic Metadata
device_type_name: "Vacuum Gauge Controller"
driver_id: "vacuum_gauge_controller_v1"
connection_type: "Serial"

# The DLL that contains the VacuumGaugeDevice and ViewModel classes
assembly_name: "VacuumGaugeController.dll"

# Serial connection specifics
serial_settings:
  requires_unit_id: false
  variable_baud_rate: false
  allowed_baud_rates: [9600]
  default_baud_rate: 9600

# Settings required when adding the device through the ARES UI
settings: []
